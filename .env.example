# ── Database (Docker internal — do not change the URL format) ──
POSTGRES_PASSWORD=change_me_strong_password

# ── App Security ───────────────────────────────────────────────
# Generate with: python -c "import secrets; print(secrets.token_hex(32))"
SECRET_KEY=change_me_to_something_long_and_random

# Admin user seeded on first startup (change after first login)
ADMIN_PASSWORD=ChangeMe123!

# ── Estimator defaults ─────────────────────────────────────────
ESTIMATOR_NAME=Austin Cantrell
ESTIMATOR_INITIALS=AC
COMPANY_LOGO_PATH=

# ── CORS — must match your public domain ───────────────────────
ALLOWED_ORIGINS=https://bids.metcalfehvac.com

# ── Microsoft Entra ID (SSO) ───────────────────────────────────
AZURE_CLIENT_ID=c7946444-64ec-4bbb-8d8a-ceabd0e28cf4
AZURE_TENANT_ID=4ca31951-6abc-4223-99fb-1686ea0e3fbc

# ── Email (Microsoft 365 SMTP) ─────────────────────────────────
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=bids@whmetcalfe.com
SMTP_PASSWORD=your_smtp_password
SMTP_FROM_NAME=WHMetcalfe HVAC Estimating
